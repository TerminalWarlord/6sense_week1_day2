export enum UserType {
    admin = "admin",
    owner = "owner",
    user = "user",
}

export enum UserStatus {
    active = "active",
    inactive = "inactive",
    banned = "banned"
}

export enum MembershipStatus {
    joined = "joined",
    invited = "invited",
    removed = "removed",
    banned = "banned",
    rejected = "rejected",
}